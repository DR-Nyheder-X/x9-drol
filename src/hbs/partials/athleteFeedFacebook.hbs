<div class="athlete-feed-entry athlete-feed-entry--facebook {{#if breaking}}athlete-feed-entry--breaking{{/if}}">
	{{>athleteFeedEntryHeader target}}
	<div class="athlete-feed-entry__content">
		<div class="athlete-feed-entry__source"><span>{{target.firstName}} på </span><h4 class="athlete-profile-social-label athlete-profile-social-label--facebook">Facebook</h4></div>
		<p>{{{data.content}}}</p>
		{{#if isNew}}
			<div class="athlete-feed-entry__new-indicator"></div>
		{{/if}}
		<div class="athlete-feed-entry__time">
			{{time}}
		</div>
	</div>
</div>
